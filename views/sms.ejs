<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SMS Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            margin: 0;
            padding: 20px;
            text-align: center;
        }
        h1 {
            color: #333;
        }
        .container {
            max-width: 900px;
            margin: auto;
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .sms-card {
            background: #fff;
            padding: 15px;
            margin: 15px 0;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            text-align: left;
        }
        .sms-card h3 {
            margin: 0;
            color: #007bff;
            font-size: 18px;
        }
        .sms-card p {
            margin: 5px 0;
            color: #333;
        }
        .sms-card .timestamp {
            font-size: 12px;
            color: #777;
        }
        .no-sms {
            color: #777;
            font-size: 18px;
            margin-top: 20px;
        }
        .back-btn {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            font-size: 16px;
            color: white;
            background: #007bff;
            text-decoration: none;
            border-radius: 5px;
            transition: 0.3s;
        }
        .back-btn:hover {
            background: #0056b3;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Received SMS</h1>

        <% if (smsData.length > 0) { %>
            <% smsData.forEach(sms => { %>
                <div class="sms-card">
                    <h3>Sender: <%= sms.sender %></h3>
                    <p><strong>Title:</strong> <%= sms.title %></p>
                    <p><strong>Body:</strong> <%= sms.body %></p>
                    <p class="timestamp"><strong>Timestamp:</strong> <%= sms.timestamp %></p>
                </div>
            <% }); %>
        <% } else { %>
            <p class="no-sms">No SMS found for this device.</p>
        <% } %>

        <a href="/api/device/dashboard" class="back-btn">Go Back</a>
    </div>

</body>
</html>
